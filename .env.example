# Log Collector Environment Configuration
# Copy this file to .env and customize for your environment

# ===================================
# Server Configuration
# ===================================

# Port on which the service will run
SERVER_PORT=7777

# ===================================
# Database Configuration
# ===================================

# Path to SQLite database file
# Default: ./data/logs.db (relative to app directory)
# Docker: /app/data/logs.db (mounted volume)
DATABASE_PATH=./data/logs.db

# ===================================
# Log Retention & Cleanup
# ===================================

# Enable automatic log cleanup
CLEANUP_ENABLED=true

# How long to keep logs (in hours)
# Default: 24 hours
# Examples: 24 (1 day), 168 (1 week), 720 (30 days)
LOG_RETENTION_HOURS=24

# Cron expression for cleanup schedule
# Default: "0 0 * * * *" (every hour at minute 0)
# Format: second minute hour day month day-of-week
# Examples:
#   "0 0 * * * *"     - Every hour
#   "0 0 0 * * *"     - Daily at midnight
#   "0 0 2 * * *"     - Daily at 2 AM
#   "0 0 */6 * * *"   - Every 6 hours
LOG_RETENTION_CRON=0 0 * * * *

# ===================================
# Authentication
# ===================================

# API Key for protecting /api/** endpoints
# Leave empty or unset to disable authentication
# Set to "auto" to auto-generate a key on startup
# Set to your own key to use a custom API key
#
# Examples:
#   API_KEY=                     # Auth disabled (default)
#   API_KEY=my-secret-key        # Use custom key
#   API_KEY=auto                 # Auto-generate key (check logs)
API_KEY=

# ===================================
# JVM Configuration (Docker only)
# ===================================

# JVM memory settings
# Adjust based on your load and available memory
JAVA_OPTS=-Xms256m -Xmx512m

# ===================================
# Future Configuration (not yet implemented)
# ===================================

# CORS allowed origins (comma-separated)
# CORS_ALLOWED_ORIGINS=*

# WebSocket enabled
# WEBSOCKET_ENABLED=true

# Maximum batch size for log ingestion
# BATCH_SIZE_LIMIT=500
